#include <stdio.h>
#include <stdlib.h>

int main()
{
    int number;

    printf("enter password:");
    scanf("%d", &number);

    if (number==69) {
        printf("nice");
    }

    else {
        printf("wrong password");
    }
return 0;
}
